# Essence API

## Описание

**Essence API** — это асинхронный REST API сервис для управления сущностями ("Essences"), разработанный на фреймворке **FastAPI**.

Проект реализует полный **CRUD** (Create, Read, Update, Delete) функционал, поддерживает валидацию данных, фильтрацию, пагинацию и массовое создание записей. В качестве базы данных используется **SQLite** (через драйвер `aiosqlite`), а для ORM — **SQLAlchemy 2.0**.

### Основные возможности

* 🚀 **Асинхронность**: Полностью асинхронные эндпоинты.
* 📄 **Документация**: Автоматическая генерация Swagger UI и ReDoc.
* 🔍 **Фильтрация и пагинация**: Поиск по названию, статусу, количеству; настройка лимита и смещения.
* 📦 **Docker**: Полная поддержка контейнеризации (Dockerfile + docker-compose).
* 🛠 **Валидация**: Строгие схемы данных на Pydantic v2.
* ✨ **Bulk Operations**: Массовое создание сущностей одним запросом.

## Стек технологий

* **Python** 3.12
* **FastAPI**
* **SQLAlchemy** (AsyncIO)
* **Pydantic** v2
* **Docker** & **Docker Compose**
* **Uvicorn**

---

## Запуск проекта

### Вариант 1: Запуск через Docker (Рекомендуемый)

Это самый быстрый способ запустить приложение, не устанавливая зависимости локально.

1.  **Клонируйте репозиторий:**
    ```bash
    git https://github.com/IlyaBackend/fastapiREST.git
    cd fastapiREST
    ```

2.  **Запустите контейнеры:**
    ```bash
    docker-compose up -d --build
    ```

3.  **Готово!** Приложение доступно по адресу:
    * API: `http://localhost:8000`
    * Документация (Swagger): [http://localhost:8000/docs](http://localhost:8000/docs)

### Вариант 2: Локальный запуск

Если вы хотите запустить проект без Docker:

1.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Windows
    python -m venv venv
    venv\Scripts\activate

    # Linux/macOS
    python3 -m venv venv
    source venv/bin/activate
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Запустите сервер:**
    ```bash
    # Через Python
    python main.py

    # Или напрямую через Uvicorn
    uvicorn main:app --reload
    ```

Приложение запустится на `http://127.0.0.1:8000`.

---

## API Эндпоинты

Полный список доступных методов можно посмотреть в Swagger UI (`/docs`).

| Метод  | URL                     | Описание                                      |
| ------ | ----------------------- | --------------------------------------------- |
| **GET** | `/api/essences`         | Получить список сущностей (фильтры, пагинация)|
| **POST** | `/api/essences`         | Создать новую сущность                        |
| **POST** | `/api/essences/bulk`    | Массовое создание сущностей                   |
| **GET** | `/api/essences/{id}`    | Получить сущность по ID                       |
| **PUT** | `/api/essences/{id}`    | Полное обновление сущности                    |
| **PATCH** | `/api/essences/{id}`    | Частичное обновление сущности                 |
| **DELETE** | `/api/essences/{id}`    | Удалить сущность                              |

## Структура проекта

```text
.
├── database.py         # Настройки подключения к БД
├── docker-compose.yml  # Конфигурация Docker Compose
├── Dockerfile          # Инструкция сборки образа
├── main.py             # Точка входа в приложение
├── models.py           # Модели базы данных (SQLAlchemy)
├── requirements.txt    # Список зависимостей
├── routers.py          # Логика эндпоинтов (View)
└── schemas.py          # Pydantic схемы (валидация)
```

## Автор 
 
**Илья Кучеревский**   
Контакт: [mbifckumm](https://t.me/mbifckumm)
